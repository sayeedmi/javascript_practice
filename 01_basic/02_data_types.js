'use strict'; // Treat all JS code as newer version

// ===> These are Premitive Data Types:

let name = 'sayeed'; // 'String' Data Type
let age = 23; // 'Number' Data Type
let married = false; // 'Boolean' Data Type
let standaloneValue = null; // 'null' is an standalone 'Value' and an 'Object' 
let noValue; // 'undifined' is a value and an 'Undefined' Data Type

//----------------------------------------------------------------------

// ==> Objects:

console.log(typeof name);
console.log(typeof(age));
console.log(typeof(married));
console.log(typeof(null));
console.log(typeof undefined);